# MySQL ??
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform= org.hibernate.dialect.MySQL8Dialect
spring.jpa.defer-datasource-initialization = true
spring.sql.init.mode = always
spring.sql.init.data-locations=classpath:/data.sql
spring.sql.init.schema-locations=classpath:/schema.sql

#RDS DB Source URL
spring.datasource.url=jdbc:mysql://database-1.cvgjkc6apvz8.ap-northeast-2.rds.amazonaws.com:3306/projectMatch?createDatabaseIfNotExist=true&useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
spring.datasource.username = admin
spring.datasource.password = pmpassword

#batch size
spring.jpa.properties.hibernate.default_batch_fetch_size=1000

#local  DB username
#spring.datasource.url=jdbc:mysql://localhost:3306/projectMatch?createDatabaseIfNotExist=true&useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=password

# true ??? JPA ??? ?? ??
spring.jpa.show-sql=true

# DDL(create, alter, drop) ??? DB? ?? ??? ??? ? ??.
spring.jpa.hibernate.ddl-auto=update

# JPA? ???? Hibernate? ????? ??? SQL? ???? ????.
spring.jpa.properties.hibernate.show_sql = true
spring.jpa.properties.hibernate.format_sql = true

# Redis
spring.redis.host=localhost
spring.redis.port=6379

# jwt
jwt.secret=${jwt_secret_key}
# 30?
jwt.access.token.expiration.millis=1000 * 60 * 30
# 7?
jwt.refresh.token.expiration.millis=1000 * 60 * 60 * 24 * 7

# jwt_test (1?, 2?)
#jwt.access.token.expiration.millis=1000 * 60
#jwt.refresh.token.expiration.millis=1000 * 60 * 2